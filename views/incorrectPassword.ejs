<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Not Found</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />

  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #1a1a1a;
      color: #fff;
      font-family: Arial, sans-serif;
    }

    .container {
      max-width: 400px;
      padding: 40px;
      background-color: #333;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    h1 {
      color: #dc3545;
      margin-bottom: 20px;
      font-size: 24px;
      font-weight: bold;
    }

    p {
      margin-bottom: 20px;
      font-size: 16px;
    }

    .login-link {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .login-link:hover {
      background-color: #e4eaf0;
      text-decoration: none;
    }

    .error-svg {
      width: 200px;
      height: 200px;
      margin-bottom: 20px;
    }

    @keyframes rotate {
      0% {
        transform: rotate(0deg);
      }

      100% {
        transform: rotate(360deg);
      }
    }

    .rotate-animation {
      animation: rotate 2s linear infinite;
    }

    .error-image {
      width: 300px;
      height: 300px;
      margin-bottom: 20px;
    }

    .error-message {
      font-size: 20px;
      margin-bottom: 20px;
    }

    .error-animation {
      animation: shake 0.5s linear infinite;
    }

    @keyframes shake {
      0% {
        transform: translateX(0);
      }

      25% {
        transform: translateX(-10px);
      }

      50% {
        transform: translateX(10px);
      }

      75% {
        transform: translateX(-10px);
      }

      100% {
        transform: translateX(0);
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>password is incorrect</h1>
    <svg class="error-svg rotate-animation error-animation error-image" xmlns="http://www.w3.org/2000/svg" width="200"
      height="200" viewBox="0 0 24 24" fill="none" stroke="#dc3545" stroke-width="2" stroke-linecap="round"
      stroke-linejoin="round">
      <circle cx="12" cy="12" r="10" />
      <line x1="12" y1="8" x2="12" y2="16" />
      <line x1="12" y1="16" x2="12" y2="16" />
    </svg>
    <p class="error-message">The password you have entered is incorrect.</p>
    <a href="/login" class="login-link">Login Again</a>
  </div>
</body>

</html>